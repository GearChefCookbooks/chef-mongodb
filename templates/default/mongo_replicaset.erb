replica_config = {
  _id: "<%= @replica_name %>",
  members: [
    <%= @mongos_replica.map {|m| "{_id: #{m[:id]}, host: \"#{m[:ip]}:#{m[:port]}\"}" }.join(',') %>
  ]
}


rs.initiate(replica_config);

sleep(10000);

